# Blank-name greeting

Preserve named greetings and return `Hello, friend!` for an empty or
whitespace-only name. Add regression coverage. Keep the existing spacing of
nonblank names; the Developer may clarify that compatibility requirement.
