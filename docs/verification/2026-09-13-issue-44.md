# Issue #44: Packaged role skills and human-invoked merge

## Implementation baseline

Runtime and packaged skills under test:
`fbf7e5ef644d40b60ccd20acdbebcc5e78da9c30`, on Increment 2's merged baseline
`5ff201a65dc3b1804aeb6c4017c7cbe5b5a82dee`.
This record accompanies [issue #44](https://github.com/MagiLand/agent-squad/issues/44)
and [draft PR #49](https://github.com/MagiLand/agent-squad/pull/49).
The subsequent evidence and guard-coverage commit changes no runtime or skill
content.

**Acceptance is pending:** the real-agent trial has been prepared, but its
Task has not received the required Developer approval. No trial implementation,
formal review, or merge has occurred in this increment. This PR must remain a
draft until the live exercise and this record are complete.

## Commands executed

Local checks use Python 3.12.11. The build-backend and style tools are in an
isolated temporary virtual environment; they add no runtime dependency.

| Command or check | Observed outcome |
| --- | --- |
| Initial focused unit and merge integration suite | 21 tests passed in 41.529 seconds. |
| Latest skill and packaging suite | 9 tests passed in 0.302 seconds. Built a wheel and source archive, then compared both packaged skill files byte for byte. |
| Initial expanded `scripts/run-smoke-tests --json` | 62 scripted CLI commands passed in 42.485 seconds. Includes both merge methods, moved-base refusal, and resource cleanup. |
| `make test PYTHON='python -B'` with the temporary environment first on `PATH` | 136 tests passed in 324.541 seconds at the implementation baseline. An earlier local run through Python version-manager shims was interrupted while progressing, without a reported test failure; it is not counted as passing. |
| `make smoke` at the committed baseline | Passed: 62 scripted CLI commands in 209.225 seconds through the slower version-manager path. |
| `make doctor` at the committed baseline | Passed; reported full HEAD `fbf7e5ef644d40b60ccd20acdbebcc5e78da9c30`, Herdr 0.9.0, protocol 22, and the live Implementer identity. |
| `quick_validate.py` for both role skills | Passed. |
| Scoped `pycodestyle --ignore=E203,E402,W503`, `pyflakes`, and `git diff --check` | Passed. |
| Independent skill forward test, followed by a focused recheck | Corrected the three instruction ambiguities below; all rechecked scenarios gave unambiguous actions. This was a read-only scenario exercise, not the required live loop. |
| `uv tool install <implementation-worktree>` | Installed the committed CLI as an isolated user tool. Its 16 runtime/data files match the baseline byte for byte. The version remains 0.4.4 because the version bump is explicitly deferred. |
| `agent-squad skill install --json` | Copied both skills to `~/.agents/skills` and created their prescribed relative Claude symlinks. Read-back confirmed exact content and link targets. No existing skill was replaced. |
| `agent-squad init --implementer-account patrickhe --reviewer-account patrick-magiland --json` in the isolated trial checkout | Initialized configuration and exclusions. The trial Implementer is named `trial-implementer-44`, separate from this repository's manual review agents. |
| `agent-squad issue view --issue 4 --json` | Read the trial Task and configured paths through the installed CLI. |
| Exact-head GitHub correctness CI at the runtime baseline | Both Ubuntu / Python 3.11 and macOS / Python 3.12 jobs passed for `fbf7e5ef644d40b60ccd20acdbebcc5e78da9c30` in [run 34796829532](https://github.com/MagiLand/agent-squad/actions/runs/34796829532). No handoff to the manual Reviewer has occurred. |
| Additional Git identity guard coverage | One targeted integration test passed in 2.150 seconds, checking foreign/common administrative paths, symlinks, back-links, the primary checkout, and wrong branch/issue placement. |

## Live trials

| Direction | Repository | Issue / PR | Reviewed heads | Review IDs | Verdicts | Decisions | Stops | Merge commit | Merge method |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Planned: Codex implements, Claude Code reviews | `MagiLand/agent-squad-trial` | [Issue #4](https://github.com/MagiLand/agent-squad-trial/issues/4); no PR yet | None | None | None | Task approval pending | None | Not merged | Planned: `merge` |

The concrete trial Task is a standard-library duration formatter with exact
formatting and invalid-input requirements, deterministic tests, and README
usage instructions. It introduces no intentional defect to manufacture a
changes-requested pass. Whether a real changes-requested pass occurs remains
to be recorded after the trial.

## Harness versions

| Tool | Version |
| --- | --- |
| Herdr | 0.9.0; protocol 22 |
| Codex CLI | 0.154.0 |
| Claude Code | 2.1.270 |
| GitHub CLI | 2.100.0 (2026-09-03) |
| Python | 3.12.11 |

## Defects found and fixed

- A new review request with an exhausted budget derives `needs_decision`.
  The initial Reviewer draft classified it as duplicate delivery before the
  budget guard. The guard now runs before that classification; a duplicate
  completed approval is still left alone.
- A same-head Task amendment can lead to two reviews with identical headers.
  Interrupted publication recovery now identifies the particular forge review
  ID, excluding reviews present before publication. An old identical header
  cannot stand in for the new publication.
- The Task-amendment route now explicitly closes the finished Reviewer before
  launching a fresh one, including when the head has not changed.
- The Reviewer frontmatter initially used angle-bracket placeholders that the
  skill validator rejects. Its description now names those fields in prose.

The CLI exposes configured primary, worktree, and scratch paths through its
read interface so the skills need neither configuration-file reads nor direct
`gh` calls. `pr create` records the designated implementation worktree in its
linked Git administrative directory. This follows the resource-ownership
approach introduced in issue #43: the record binds checkout identity and the
PR, and stores no review, verdict, budget, decision, or handoff status.

Merge reads visible branch protections and rules, passes the approved head to
GitHub, and verifies fetched integration before cleanup. The adapter follows
the documented [merge endpoint](https://docs.github.com/en/rest/pulls/pulls#merge-a-pull-request)
and [branch-protection endpoint](https://docs.github.com/en/rest/branches/branch-protection#get-branch-protection).
A plan that does not expose branch rules is reported as `no rule visible`;
authentication and rate-limit failures remain errors.

## Scope and limitations

Automated forge reviews, findings, decisions, stops, and Herdr process states
are scripted. The fake forge performs real Git merges in disposable clones.
The smoke exercises moved-base refusal, explicit acceptance with ancestry
verification, and a second PR squash-merged on an unmoved base with tree
identity verification. It verifies that the base checkout stays unchanged.
Publication-recovery smoke cases use an independent disposable PR after the
two successful merges so they cannot invalidate those completed approvals.

A squash after accepting a moved base is reported as `integration not
verifiable by tree identity`, returns exit 1, and retains resources. A failed
cleanup step returns exit 3 and reports its completed and failed steps.
Uncommitted implementation files, mismatched ownership, unowned review
worktrees, and a symlinked scratch directory are retained.

The real loop, its human-gated merge, and final acceptance evidence remain
unfinished. Doctor feature changes, the README rewrite, the version bump,
both-direction trials, the consumer trial, and rebase merging remain outside
this increment. Agent Squad's own PR uses the manual review pipeline.

## Cleanup

Successful automated merge scenarios removed their issue worktree, remote and
local feature branch, review worktrees, and per-PR scratch directory. The
fixtures remove their disposable roots on exit. Failure scenarios verified
retention rather than performing broad cleanup.

Retained for the live exercise: `/private/tmp/agent-squad-issue44-trial`, trial
issue #4, the installed CLI and both skills, and the issue #44 development
branch/worktree. No new real trial agent or Reviewer workspace has been
started. Existing trial issue #1, open PR #2, and unrelated worktrees were left
unchanged.

Private validation logs and the read-only skill-scenario report are under
`/private/tmp/issue44-*` and
`/private/tmp/agent-squad-skill-forward-test-20260913.md`. The pretrial evidence archive is
`/private/tmp/issue44-pretrial-evidence.tar.gz`, SHA-256
`c7c1930861948bb01b07b2d78cd51f49386c26a2b4e0435c4444dcb0619c60f7`. The final record will add the live
trial evidence archive and its digest.
