# Issue #16 verification — 2026-09-05

Implementation baseline: `d4a6e303931f3233d7016beb1e88fc1da77297e4`.
The candidate changes smoke tooling, fixtures, tests, and documentation; it does
not change production runtime source or dependencies.

## Commands executed

| Command | Observed outcome |
| --- | --- |
| `make test` before the change | Passed, 411 tests in 565.712 seconds |
| `make test` with the new scenario | Passed, 413 tests in 549.639 seconds |
| `python3 -m unittest tests.integration.test_smoke_workflow` | Passed, 2 tests in 5.675 seconds |
| `make smoke` | Passed; complete two-round artifact workflow, fake Herdr, no models, no retained resources |
| `python3 scripts/run-smoke-tests --json` | Passed, 36 recorded commands; task digest `716a620e280fec32116400424957c165193bd6bcb4cf365453e066759df79ba1` |
| `python3 -m pycodestyle tests/smoke_workflow.py tests/integration/test_smoke_workflow.py scripts/run-smoke-tests scripts/prepare-live-smoke` | Passed |
| `python3 -m pyflakes tests/smoke_workflow.py tests/integration/test_smoke_workflow.py scripts/run-smoke-tests scripts/prepare-live-smoke` | Passed |

`make smoke` also passed in a complete `git archive` export of
`5e490a2878ad4a054917881b17b8346e95a79a0d`, with no `.git`, `.local`, retained
smoke resources, or generated Python bytecode. Reproduce with an owned temporary
directory as `EXPORT_ROOT`:

```bash
git archive 5e490a2878ad4a054917881b17b8346e95a79a0d --output "$EXPORT_ROOT/source.tar"
tar -xf "$EXPORT_ROOT/source.tar" -C "$EXPORT_ROOT/export"
make -C "$EXPORT_ROOT/export" smoke
```

Create the empty `export` subdirectory first. Remove only the owned export root
after verification.

The script also passed from a temporary source export containing only `src/`,
the smoke script/module, `tests/__init__.py`, and committed smoke/fake-Herdr
fixtures, invoked from outside that export. The export had no `.git` or `.local`
and acquired no Git directory or Python cache files.

## Live trials

Herdr version observed: `0.8.2`; Codex CLI `0.153.4`; Claude Code `2.1.261`.
Both trial repositories were created by
`scripts/prepare-live-smoke`, with clean fixture commits and captured task/context
before any candidate change.

| Direction | Run ID | Base commit | Outcome |
| --- | --- | --- | --- |
| Codex implements, Claude Code reviews | `3abbcf49-87cd-4f25-86c6-78436994c578` | `3b101d211598ca43c58ba6a480b73c320c79f366` | Pending native directory-trust confirmation; no candidate or review submitted |
| Claude Code implements, Codex reviews | `bc82a78b-d732-4dd9-87f4-17d01631aa27` | `7c7799f3f04fd17cc2fe75af01aff4158f60c6ff` | Pending native directory-trust confirmation; no candidate or review submitted |

The first Codex launch exited before changing the fixture. A retry with terminal
diagnostics exposed the native directory-trust prompt. Claude startup returned
Herdr `agent_not_ready` at its equivalent prompt. The Herdr skill requires asking
the Developer before answering a blocked UI. Neither prompt has been accepted by
the driver. No live review or completion is claimed from preparation or startup.

The provisional consuming repository is a `--no-hardlinks` clone of
`MagiLand/double-dubs` at `3251ee3abd769aa0f4ed4874e65a1c2d14fc953e`, on
`chore/agent-squad-trial`. Run `90fd7892-075c-46e9-8738-fa0f212069c1` captures
a documentation-only task to describe optional local Agent Squad usage, link it
from the docs index, and validate links and whitespace. No app changes, build,
review, or completion are claimed. The original checkout was not modified.

Prepared resources retained for continuation (all under this machine's temporary
directory; each root contains `trial.json`):

| Root name | Herdr workspace / pane | Purpose |
| --- | --- | --- |
| `agent-squad-live-6jlrt25c` | `w3` / `w3:p1` | Codex-to-Claude fixture |
| `agent-squad-live-663_xp5y` | `w4` / `w4:p1` | Claude-to-Codex fixture |
| `agent-squad-consumer-l5jry1u9` | `w5` / `w5:p1` | Double Dubs clone |

## Open acceptance work and limitations

- Complete both real directions, record exact review identities and cleanup.
- Demonstrate the supported live recovery/human-resolution paths in Section 36.6,
  and record any deviation explicitly.
- Complete the real consuming-repository trial; preserve the original checkout.
- Scripted deterministic reviews/resolutions prove protocol behavior only.
  They do not establish independent model judgment, actual Developer approval,
  or independent self-review of Agent Squad.
- Live trial directories and their owned Herdr workspaces are retained pending
  native trust confirmation. No generated review bundles are committed.
