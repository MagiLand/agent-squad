# Issue #16 verification — 2026-09-05

Implementation baseline: `d4a6e303931f3233d7016beb1e88fc1da77297e4`.
The candidate adds smoke tooling, fixtures, tests, and documentation. Live trials
also exposed and fixed explicit source-repository selection in the Herdr adapter
(commit `dded8ac875b046b5a54340b0e07c70e8cd6007cd`). Runtime dependencies and the
single-active-run boundary remain unchanged.

## Commands executed

| Command | Observed outcome |
| --- | --- |
| `make test` before the change | Passed, 411 tests in 565.712 seconds |
| `make test` with the new scenario | Passed, 413 tests in 549.639 seconds |
| `make test` after the live-trial adapter fix | Passed, 413 tests in 579.457 seconds |
| `python3 -m unittest tests.integration.test_smoke_workflow` | Passed, 2 tests in 5.675 seconds |
| `make smoke` | Passed; complete two-round artifact workflow, fake Herdr, no models, no retained resources |
| `python3 scripts/run-smoke-tests --json` | Passed, 36 recorded commands; task digest `716a620e280fec32116400424957c165193bd6bcb4cf365453e066759df79ba1` |
| `python3 -m pycodestyle tests/smoke_workflow.py tests/integration/test_smoke_workflow.py scripts/run-smoke-tests scripts/prepare-live-smoke src/agent_squad/herdr.py tests/fixtures/fake_herdr.py tests/integration/test_herdr.py` | Passed |
| `python3 -m pyflakes tests/smoke_workflow.py tests/integration/test_smoke_workflow.py scripts/run-smoke-tests scripts/prepare-live-smoke src/agent_squad/herdr.py tests/fixtures/fake_herdr.py tests/integration/test_herdr.py` | Passed |

`make smoke` also passed in a complete `git archive` export of
`dded8ac875b046b5a54340b0e07c70e8cd6007cd`, with no `.git`, `.local`, retained
smoke resources, or generated Python bytecode. Reproduce with an owned temporary
directory as `EXPORT_ROOT`:

```bash
git archive dded8ac875b046b5a54340b0e07c70e8cd6007cd --output "$EXPORT_ROOT/source.tar"
tar -xf "$EXPORT_ROOT/source.tar" -C "$EXPORT_ROOT/export"
make -C "$EXPORT_ROOT/export" smoke
```

Create the empty `export` subdirectory first. Remove only the owned export root
after verification.

The script also passed from a temporary source export containing only `src/`,
the smoke script/module, `tests/__init__.py`, and committed smoke/fake-Herdr
fixtures, invoked from outside that export. The export had no `.git` or `.local`
and acquired no Git directory or Python cache files.

## Runtime defect found by the live trials

From the Codex fixture's working directory, `herdr worktree list` selected the
focused Agent Squad workspace and omitted the fixture's valid review worktree.
`herdr worktree list --cwd <fixture-repository>` selected the correct repository
and listed that same registered detached worktree. Submission had preserved run
`3abbcf49-87cd-4f25-86c6-78436994c578`, round 1, request
`0145fcce-62e7-47f8-b67e-87b23a26ec23`; its bundle was intact.

`HerdrClient` now passes `--cwd <implementation-repository>` to `worktree open`
and requires that option in the discovered CLI/schema contract. The fake models
a different focused repository; its regression failed with `worktree path not
found` before the fix, then passed with it. `retry-handoff` reached the correct
workspace without replacing the preserved request. The focused command
`python3 -m unittest tests.integration.test_herdr tests.unit.test_herdr tests.integration.test_smoke_workflow`
passed 14 tests in 7.910 seconds. The full suite and committed-export smoke also
passed after this fix, as recorded above.

## Live trials

Herdr `0.8.2`, Codex CLI `0.153.4`, and Claude Code `2.1.261` were used.
Implementers and Reviewers were separate real harness sessions in owned temporary
repositories. Directory trust was authorized by the Developer. Reviewers chose
verdicts from the captured tasks, code, and evidence; the driver did not write
their review artifacts. Review commands used the exposed candidate CLI. No
runtime state, marker, request, or review result was hand-edited to advance a run.

| Trial | Run ID | Base | Reviewed candidate | Outcome |
| --- | --- | --- | --- | --- |
| Codex implements, Claude reviews | `3abbcf49-87cd-4f25-86c6-78436994c578` | `3b101d211598ca43c58ba6a480b73c320c79f366` | Initial `b553924969e7dc41ecfbbb34c882a36bf758b672`; correction `dd0d691a589a65e8fac2d6d8224dfc632694168f` | Approved and completed |
| Claude implements, Codex reviews | `bc82a78b-d732-4dd9-87f4-17d01631aa27` | `7c7799f3f04fd17cc2fe75af01aff4158f60c6ff` | `a67848d58f0d81ed9a598d4623d4e7bc46087527` | Approved and completed |
| Same-SHA evidence reconsideration | `08f65832-bcb5-4eae-aeba-5f5f319b5f7a` | `a67848d58f0d81ed9a598d4623d4e7bc46087527` | `ef9f2eb5ab3773e8d4f94bab8dab4b898f2f5893` | Approved and completed at unchanged HEAD |
| Double Dubs consumer | `90fd7892-075c-46e9-8738-fa0f212069c1` | `3251ee3abd769aa0f4ed4874e65a1c2d14fc953e` | `dd10ce0d8f873f2ac22aff7c0a4715a759bccc36` | Approved and completed |

### Correction, escalation, and resolution

Claude independently returned `changes_requested`, result
`ccc67b80-dcb7-41a0-a7d8-dde03ac759af`, for the intentionally incomplete first
Codex checkpoint. Both findings were valid: whitespace-only input lacked the
fallback, and regression coverage omitted whitespace and preserved name spacing.
The driver applied the result; Codex read the archive, evaluated the findings,
and called `escalate --response ../escalation-response.json` with both findings
marked `needs_human` for the explicit scripted exercise.

The driver called `resume --resolution ../resolution.md --applies-to-finding
REV-001 --applies-to-finding REV-002 --extend-rounds 1`. Resolution
`8a4225e6-3d37-4413-b6ef-1e0571257e18` resolves escalation
`e12dfe0d-fc34-4d28-9cf1-d5d11b1b039b` and increases the effective limit from
four to five. The decision confirms the already-captured requirements and is
explicitly a **scripted fixture decision**, not an actual unresolved product
choice, task amendment, or human approval of Agent Squad. Codex demonstrated
five failing whitespace cases before correction and four passing tests after it.
The replacement response `e1371b23-5549-46bd-ac69-1ffb1f706e9a` superseded
`0e7377de-0751-4a35-a693-f4a8db1c8d7e` and linked the resolution. The driver
verified byte-for-byte preservation of the old response in diagnostics, the new
response in canonical storage and the fresh bundle, and the resolution JSON and
Markdown in canonical storage and the fresh bundle. The archived old response
SHA-256 is `5ffb81b002b3749da4f9c6e55c7fc201cb00d9713605e0ea7e8a7e1f67a28e70`;
the resolution Markdown SHA-256 is
`4bcb58074904c62396ff8d65766d881b89b0858d96591b71c4bbfbea29584545`.

Fresh request `1e7b1feb-4a5d-4bcf-9bbd-b1987dd210eb` received independent Claude
approval `5c34a13c-f85f-41b2-bf5a-6d675ed75c6e` at
`dd0d691a589a65e8fac2d6d8224dfc632694168f`. Claude verified the four passing tests,
the five failures with the old source, the resolution digest and unchanged
requirements, and the response replacement linkage. The driver applied this
exact result and completed the run. Its two review workspaces and implementation
workspace were closed; the clean temporary repository was archived and removed.

### Reviewer exit, supersession, and lost notification

The first reverse-direction request was
`11c286cd-eba0-4187-921a-b912131e7518`. The driver exited only its owned idle
Codex Reviewer through `herdr agent send-keys ... ctrl+d`, confirmed
`agent_not_found`, and called `retry-handoff`. Recovery relaunched the Reviewer
with that same run, round, request, and reviewed head. The driver then called
`supersede --reason <recorded smoke purpose>` and submitted the unchanged head as
a fresh `new_revision` round, which is permitted after supersession. Round 2 had
request `44b9ec87-c8bf-4a2d-856f-09ea980417b4` and a fresh Reviewer.

Codex independently approved the candidate after all four tests passed, result
`cb1b4a19-0abd-4fe3-a5e7-bb90fad3b320`. Its normal `review-submit` created the
valid marker, but notification failed because the harness could not access the
Herdr socket. The driver had also temporarily renamed the owned Implementer to
exercise absence. A privileged `review-submit` retry reused the same marker and
failed specifically because the configured Implementer was unavailable.

With no artifact edits, `status` reported the exact ready result and apply
command; `retry-handoff` returned `use marker-confirmed result`. The driver restored
the Implementer name, called `apply-review --result-id
cb1b4a19-0abd-4fe3-a5e7-bb90fad3b320`, and `complete` released the active slot at
`a67848d58f0d81ed9a598d4623d4e7bc46087527`.

### Same-SHA evidence reconsideration

After the reverse-direction run completed, the same clean fixture started a new
run using the committed `tests/fixtures/smoke/reconsideration-task.md`. It requires
`GREETING_CONTRACT.md` and an implementation-owned execution record at the exact
reviewed HEAD. Claude committed only the documentation and honestly disclosed
that the first report omitted the execution record. No source defect was
invented to force the scenario.

Initial request `4e08285d-02c9-461f-bbf1-9790c8cbd2c1` received real Codex result
`d4a43293-d9c7-45b0-8864-eba94150fb3a`, `changes_requested`, with one valid missing
evidence finding. The Reviewer confirmed the code and documentation were correct
and that no source or test change was needed. After the driver applied the result,
Claude executed these checks at unchanged HEAD
`ef9f2eb5ab3773e8d4f94bab8dab4b898f2f5893`:

| Command | Exit | Actual result |
| --- | --- | --- |
| `python3 -B -m unittest` | 0 | Four tests passed |
| `python3 -B -c 'from greeting import greet; print(repr(greet("")))'` | 0 | `'Hello, friend!'` |
| `python3 -B -c 'from greeting import greet; print(repr(greet(" \t\n ")))'` | 0 | `'Hello, friend!'` |
| `python3 -B -c 'from greeting import greet; print(repr(greet(" Ada ")))'` | 0 | `'Hello,  Ada !'` |

Response `44a10e37-69a7-4e8c-aefa-a0435b2b1934` acknowledges the original finding
was valid and includes actual commands, outputs, exit codes, and revision/blob
identity checks. Its `rejected` disposition uses the protocol's reconsideration
path to explain why the existing revision now satisfies the task; it does not
claim the first report contained evidence. Claude submitted the response and
updated report with `--mode reconsideration`, creating fresh request
`72941967-6b1e-4079-a313-30ef1118ee0e` at the same full HEAD. Fresh Codex approval
`3bfb7fb8-4016-4e35-81db-4bad3af87fb3` closed the evidence finding and found no
remaining issues. It independently repeated the suite and direct checks, verified
input digests and previous-response identities, and confirmed only documentation
changed from the base. The review JSON SHA-256 is
`7a5e89f97ef03ba6f3105eeb5b4b253d1e7331f562a4b424d36567b84a9b304e`; the new
implementation evidence report SHA-256 is
`1842a36c69aeac1a96c71d4482fac1d75ea8332f5740e6913114e3bb5669ffbd`.

Native Reviewer notification again failed on the Herdr socket, leaving a valid
marker. The driver discovered the exact result with `status`, applied it, and
completed the run. Both applied round records retain
`ef9f2eb5ab3773e8d4f94bab8dab4b898f2f5893`. The clean fixture, including both
completed runs, was archived and removed after its owned workspaces were closed.

### Real consuming repository

The consumer was a `--no-hardlinks` clone of `MagiLand/double-dubs` at the exact
base above, on `chore/agent-squad-trial`. Real Codex added
`docs/agents/agent-squad.md` and one index link in `docs/README.md` (107 added
lines). Real Claude verified the requested command guidance against the candidate
CLI and consumer Makefile, all 18 relative Markdown links, and `git diff --check`.
It approved result `030f0ac2-9a0f-46d3-940b-8393307a7dfd`. Its three advisory
observations about wording and optional correction-path detail did not require
changes to the approved task.

The driver applied the exact approval and completed the run. `git ls-files
.agent-squad .agent-squad-review` returned no paths, final Git status was clean,
and only the implementation checkout remained registered. The verified owned
workspaces `w8` and `w5` were closed; the entire temporary clone was archived and
removed. No consumer branch or commit was pushed, and the original checkout was
not modified. This is a documentation workflow trial, not app-build or release
validation.

## Evidence retention and cleanup

Private raw evidence is retained outside the repository at
`/private/tmp/agent-squad-issue16-evidence-djrnbx3q`. It includes runtime artifacts
and therefore is intentionally **not committed**. These archives contain the
owned repositories, canonical run and round records, captured inputs, independent
reviews, driver command/exit records, and supplied reports and responses:

| Archive | SHA-256 |
| --- | --- |
| `codex-trial.tar.gz` | `976b5e2ddc65a834692f3c0445767183f38bb86b7df179a6d775dc3a0cc637e5` |
| `claude-trials.tar.gz` | `7fbd1c47d2ea7016880fa168c0eb434e3a316924eec0b7d03bb5db01c9ca0142` |
| `consumer-trial.tar.gz` | `d1d4a4b9c0865ba505372e8b8b75d0a584ab68dc55fd0123eebc0254e0677d12` |

Each archive was read back before removing its owned root. The adjacent cleanup
JSON records confirm no active run, clean Git status, one remaining registration
(the implementation checkout), and root removal. All ten owned trial workspaces
were closed: `w3`, `w4`, `w5`, `w6`, `w7`, `w8`, `w9`, `wA`, `wB`, and `wC`.
Unrelated development workspaces were preserved. No live trial resources remain;
only the private evidence archives and their summaries are retained.

## Scope and limitations

All issue #16 acceptance paths and specification Section 36.6 live scenarios
completed through supported commands. The adapter correction implements the
existing requirement to select the intended repository; it adds no runtime
dependency, concurrency mechanism, or new protocol. There is no deliberate
specification deviation.

The Developer resolution was an explicitly scripted decision confirming the
fixture's existing requirements. Native directory trust was manually accepted,
and shell startup timing required supported request retries. Native sandbox
notification failures were recovered through marker discovery. These are
supervised workflow trials, not unattended native harness startup proof.

The consumer trial validates documentation changes, not an application build or
release. The real trials reviewed fixture and consumer changes. No claim of
independent self-review of Agent Squad is made: this implementation requires its
own external PR review before approval and merge.
